rule tests:
    message: "Running unit tests..."
    conda: "../workflow/rules/envs/python3_v6.yaml"
    shell:
        r"""
        PYTHONPATH=workflow/scripts \
        python -m unittest discover -s tests -v
        """